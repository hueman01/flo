# YellowFlores

# para ejecutar en bach start rosas.html

# para subir al repositorio de la carpeta iniciar git y agregar estos comandos

git init
git add .
git commit -m "Hacer pagina responsive"

# conectar repositorio

git remote add origin https://github.com/TU_USUARIO/TU_REPOSITORIO.git

# subir proyecto

git branch -M main
git push -u origin main